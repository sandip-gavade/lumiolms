NEXT_PUBLIC_API_URL=http://localhost:3001
# Fixed dev tenant id — seeded by lumio-be's `npm run seed:demo` (see prisma/seed-demo.ts).
# Sent as the `x-tenant-id` header on every request so local/docker-compose dev doesn't need
# subdomain-based tenant routing.
NEXT_PUBLIC_TENANT_ID=00000000-0000-0000-0000-000000000001
